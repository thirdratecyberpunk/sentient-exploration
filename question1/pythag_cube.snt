# calculating pythagorean cubes
int10 x, y, z, s, s_max;

# all values are greater than zero and c is less than the maximum specified
invariant x > 0, y > 0, z > 0, s > 0, s < s_max;
# x^2 + y^2 + z^2 = s^2
invariant square(s) == square(x) + square(y) + square(z);

expose x, y, z, s, s_max;
