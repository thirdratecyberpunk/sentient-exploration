# solution to first question in Sentient worksheet
int a, b, c, c_max;

# all values are greater than zero and c is less than the maximum specified
invariant a > 0, b > 0, c > 0, c < c_max;
# a^2 + b^2 = c^2
invariant square(c) == square(a) + square(b);

expose a, b, c, c_max;
